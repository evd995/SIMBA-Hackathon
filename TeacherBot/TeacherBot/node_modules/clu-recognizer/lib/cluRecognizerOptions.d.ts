import { TurnContext, Activity, RecognizerResult } from 'botbuilder';
import { DialogContext } from 'botbuilder-dialogs';
import { HttpClient } from '@azure/ms-rest-js';
import { CluApplication } from './cluApplication';
import { CluRecognizerOptionsBase, CluRecognizerOptionsBaseFields } from './cluRecognizerOptionsBase';
/**
 * Options for CluRecognizerOptions.
 */
export declare class CluRecognizerOptions extends CluRecognizerOptionsBase {
    /**
     * Initializes a new instance of the CluRecognizerOptions class.
     * @param application The CLU application to use to recognize text.
     * @param fields The fields to load to the base class.
     */
    constructor(application: CluApplication, fields?: CluRecognizerOptionsBaseFields);
    /**
     * @inheritdoc
     */
    recognize(utterance: string, httpClient: HttpClient): Promise<RecognizerResult>;
    /**
     * @inheritdoc
     */
    recognize(turnContext: TurnContext, httpClient: HttpClient): Promise<RecognizerResult>;
    /**
     * @inheritdoc
     */
    recognize(dialogContext: DialogContext, activity: Activity, httpClient: HttpClient): Promise<RecognizerResult>;
    private recognizeWithTurnContext;
    private recognizeWithUtterance;
    private getCluResponse;
    private buildRequestBody;
    private buildRecognizerResultFromCluResponse;
    private buildUri;
}
